<link rel="stylesheet" type="text/css" href="../../styles/style.css" />
<link rel="stylesheet" type="text/css" href="../../assets/fonts/font-awesome/css/font-awesome.min.css" />
<script src='../../script/alerts.js'></script>
<title>Dashboard - LenoxBot</title>
<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a href="https://lenoxbot.com" class="navbar-brand text">LenoxBot</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
            aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto" id="mainnavbar">
                <li class="nav-item">
                    <a href="../../home" class="h5 nav-link">Home</a>
                </li>
                <li class="nav-item">
                    <a href="../../documentation" class=" h5 nav-link">Documentation</a>
                </li>
                <li class="nav-item">
                    <a href="https://medium.com/lenoxbot" target="blanc" class=" h5 nav-link">Blog
                        
                    </a>
                </li>
                <li class="nav-item">
                    <a href="https://lenoxbot.com/invite" target="blanc" class=" h5 nav-link">Invite LenoxBot</a>
                </li>
                <li class="nav-item">
                    <a href="https://lenoxbot.com/discord" target="blanc" class=" h5 nav-link">Discord Server</a>
                </li>
                <li class="nav-item active">
                    {{#if user}}
                    <a href="../../servers" class=" h5 nav-link">Dashboard
                        <span class="badge badge-danger">BETA</span>
                    </a>
                    {{/if}}
                </li>
            </ul>
            <ul class="navbar-nav navbar-right" id="mainnavbar">
                <li class="nav-item">
                    {{#if user}}
                    <a href="../../logout" class=" h5 nav-link ">Log out</a>
                    {{ else }}
                    <a href="../../loginpressedbutton" class=" h5 nav-link ">Log in</a>
                    {{/if}}
                </li>
                <li class="avatarnavigation">
                    {{#if user}}
                    <a href="https://lenoxbot.com/servers">
                        <img class="img-fluid rounded profilepic-navbar nav-link img-thumbnail" src="https://cdn.discordapp.com/avatars/{{user.id}}/{{user.avatar}}.jpg"
                            height="30" width="50">
                    </a>
                    {{/if}}
                </li>
            </ul>
        </div>
    </nav>
    </div>
</header>

<main>
    {{#if submitlogs}}
    <div class="alert alert-success alert-dismissible alert-savedsettings" id="success-alert" style="float:right;">
        <button type="button" class="close" data-dismiss="alert" aria-label="close" aria-hidden="true">&times;</button>
        <span class="text"></span>
        <strong>New Logging settings saved!</strong>
        <hr class="message-inner-separator">
        <p>
            We saved your new Logging settings for {{guilds.name}}</p>
    </div>
    {{/if}}
    <div class="container">
       <div class="row">
			<div class="col-sm-3">
				<p class="text text-white h3" style="text-align: center;" id="guildsname-dashboard">{{guilds.name}}</p>
                <nav class="navbar navbar-dark bg-dark navbar-expand-lg" id="dashboardnav">
                    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#dashboardnavigationbar" aria-controls="dashboardnavigationbar"
					    aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>
					<div class="collapse navbar-collapse" id="dashboardnavigationbar">
                    <ul class="navbar-nav mr-auto flex-md-column" id="mainnavbar">
                        <li class="nav-item nav-link">
                            <a href="/dashboard/{{guilds.id}}/overview" class=" h5 nav-link" role="button">Overview</a>
                        </li>
                        <li class="nav-item nav-link">
                            <a href="/dashboard/{{guilds.id}}/generalsettings" class=" h5 nav-link" role="button">General Settings</a>
                        </li>
                        <li class="nav-item nav-link">
                            <a href="/dashboard/{{guilds.id}}/logs" class=" h5 nav-link active" role="button">Logssettings</a>
                        </li>
                        <li class="nav-item nav-link">
                            <a href="/dashboard/{{guilds.id}}/modules" class=" h5 nav-link" role="button">Modules</a>
                        </li>
                        <li class="nav-item nav-link">
                            <a href="/dashboard/{{guilds.id}}/chatfilter" class=" h5 nav-link" role="button">Chatfilter</a>
                        </li>
                        <li class="nav-item nav-link">
                            <a href="/dashboard/{{guilds.id}}/music" class=" h5 nav-link" role="button">Music</a>
                        </li>
                        <li class="nav-item nav-link">
                            <a href="/dashboard/{{guilds.id}}/application" class=" h5 nav-link" role="button">Application</a>
                        </li>
                        <li class="nav-item nav-link">
                                <a href="/dashboard/{{guilds.id}}/tickets" class=" h5 nav-link " role="button">Tickets</a>
                            </li>
                            <li class="nav-item nav-link">
                                <a href="#" class="h5 nav-link" role="button">Custom Commands <span class="badge badge-warning">Incoming!</span></a>
                        </li>
                    </ul>
                </nav>
            </div>
            <div class="col-sm-9">
                <div class="panel-group-dashboard">
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Modlog</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="modlog">Modlog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="modlog">
                                                {{#if confs.modlogset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="modlogchannel">New Modlog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="modlogchannel">
                                                {{#each channels}} {{#if modlogset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Chatfilter</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="chatfilterlog">Chatfilterlog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="chatfilterlog">
                                                {{#if confs.chatfilterset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="chatfilterlogchannel">New Chatfilter channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="chatfilterlogchannel">
                                                {{#each channels}} {{#if chatfilterset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Messagedelete</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="messagedeletelog">Messagedeletelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="messagedeletelog">
                                                {{#if confs.messagedeleteset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="messagedeletelogchannel">New Messagedeletelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="messagedeletelogchannel">
                                                {{#each channels}} {{#if messagedeleteset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Messageupdate</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="messageupdatelog">Messageupdatelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="messageupdatelog">
                                                {{#if confs.messageupdateset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="messageupdatelogchannel">New Messageupdatelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="messageupdatelogchannel">
                                                {{#each channels}} {{#if messageupdateset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Channelupdate</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="channelupdatelog">Channelupdatelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="channelupdatelog">
                                                {{#if confs.channelupdatelog}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="channelupdatelogchannel">New Channelupdatelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="channelupdatelogchannel">
                                                {{#each channels}} {{#if channelupdateset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Channelcreate</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="channelcreatelog">Channelcreatelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="channelcreatelog">
                                                {{#if confs.channelcreatelog}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="channelcreatelogchannel">New Channelcreatelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="channelcreatelogchannel">
                                                {{#each channels}} {{#if channelcreateset}}
                                                <option>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Channelupdate</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="channelupdatelog">Channelupdatelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="channelupdatelog">
                                                {{#if confs.channelupdateset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="channelupdatelogchannel">New Channelupdatelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="channelupdatelogchannel">
                                                {{#each channels}} {{#if channelupdateset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Channeldelete</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="channeldeletelog">Channeldeletelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="channeldeletelog">
                                                {{#if confs.channeldeleteset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="channeldeletelogchannel">New Channeldeletelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="channeldeletelogchannel">
                                                {{#each channels}} {{#if channeldeleteset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Memberupdate</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="memberupdatelog">Memberupdatelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="memberupdatelog">
                                                {{#if confs.memberupdateset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="memberupdatelogchannel">New Memberupdatelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="memberupdatelogchannel">
                                                {{#each channels}} {{#if memberupdateset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Presenceupdate</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="presenceupdatelog">Presenceupdatelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="presenceupdatelog">
                                                {{#if confs.presenceupdateset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="presenceupdatelogchannel">New Presenceupdatelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="presenceupdatelogchannel">
                                                {{#each channels}} {{#if presenceupdateset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Userjoin</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="welcomelog">Userjoinlog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="welcomelog">
                                                {{#if confs.welcomelogset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="welcomelogchannel">New Userjoinlog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="welcomelogchannel">
                                                {{#each channels}} {{#if welcomelogset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Userleft</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="byelog">Userleftlog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="byelog">
                                                {{#if confs.userleftset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="byelogchannel">New Userleftlog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="byelogchannel">
                                                {{#each channels}} {{#if userleftset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Rolecreate</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="rolecreatelog">Rolecreatelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="rolecreatelog">
                                                {{#if confs.rolecreateset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="rolecreatelogchannel">New Rolecreatelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="rolecreatelogchannel">
                                                {{#each channels}} {{#if rolecreateset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Roledelete</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="roledeletelog">Roledeletelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="roledeletelog">
                                                {{#if confs.roledeleteset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="roledeletelogchannel">New Roledeletelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="roledeletelogchannel">
                                                {{#each channels}} {{#if roledeleteset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Roleupdate</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="roleupdatelog">Roleupdatelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="roleupdatelog">
                                                {{#if confs.roleupdateset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="roleupdatelogchannel">New Roleupdatelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="roleupdatelogchannel">
                                                {{#each channels}} {{#if roleupdateset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class="card border-dark panel-dashboard">
                            <div class="card-header bg-primary">
                                <p class="text text-white h3">Guildupdate</p>
                            </div>
                            <div class="card-body">
                                <form class="form-vertical" id="newmodlog-form" action="/dashboard/{{guilds.id}}/logs/submitlogs" method="POST">
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="guildupdatelog">Guildupdatelog Status:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-size="7" name="guildupdatelog">
                                                {{#if confs.guildupdateset}}
                                                <option value="true" selected>Activate</option>
                                                <option value="false">Deactivate</option>
                                                {{else}}
                                                <option value="true">Activate</option>
                                                <option value="false" selected>Deactivate</option>
                                                {{/if}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="control-label col-sm" for="guildupdatelogchannel">New Guildupdatelog channel:</label>
                                        <div class="col-sm-4">
                                            <select required class="form-control custom-select custom-select-lg mb-3 selectpicker" data-live-search="true" data-size="7"
                                                name="guildupdatelogchannel">
                                                {{#each channels}} {{#if guildupdateset}}
                                                <option selected>{{name}}</option>
                                                {{else}}
                                                <option>{{name}}</option>
                                                {{/if}} {{/each}}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-offset-2 col-sm-10">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
            </div>
        </div>
    </div>
</main>
